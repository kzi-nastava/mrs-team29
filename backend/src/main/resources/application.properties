spring.application.name=Driverr

# SERVER
server.port=8081

# DATABASE
spring.datasource.url=jdbc:postgresql://localhost:5432/driverr
spring.datasource.username=postgres
spring.datasource.password=123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MAP / GEO
map.nominatim.base-url=https://nominatim.openstreetmap.org
map.osrm.base-url=http://router.project-osrm.org
map.user-agent=Driverr/1.0 (Neytan269, njevremovic01@gmail.com)
map.nominatim.email=njevremovic01@gmail.com
map.referer=http://localhost:4200
# EMAIL CONFIGURATION
# Gmail SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:njevremovic01@gmail.com}
spring.mail.password=${MAIL_PASSWORD:eiijibjfeneqktev}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Backend URL for email links
# Mobile testing: Use your computer's LAN IP (e.g., 192.168.0.12:8081)
# Web testing: Use localhost (http://localhost:8081)
# Set via environment variable: BACKEND_URL=http://192.168.0.12:8081
# Current IP for mobile app testing: 192.168.0.12
app.backend.url=${BACKEND_URL:http://192.168.0.12:8081}

# JWT
app.jwt.secret=${APP_JWT_SECRET:driverr-super-secret-key-change-this-in-production-1234567890abcdef}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}

# OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# RECOMMENDED: Set environment variables MAIL_USERNAME and MAIL_PASSWORD instead of hardcoding
